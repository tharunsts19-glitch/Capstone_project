version: '3.8'

services:
  pd-recommender:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: streamlit run src/app/app.py --server.address 0.0.0.0
